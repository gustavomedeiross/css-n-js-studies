One of the most important properties of the window object is the property "document".

The object document contains all the HTML of the page.

console.log(document);  //this will show all the HTML of that page
